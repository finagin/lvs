version: '3'

services:

  traefik:
    image: traefik:2.5
    container_name: traefik
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.traefik.rule=Host(`traefik.${DOMAIN:-test}`)'
      - 'traefik.http.routers.traefik.service=api@internal'
      - 'traefik.http.routers.traefik.entrypoints=websecure'
      - 'traefik.http.routers.traefik.tls=true'
      - 'traefik.http.services.traefik.loadbalancer.server.port=8080'
    restart: always
    environment:
      CF_DNS_API_TOKEN: ${CLOUDFLARE_TOKEN}
      CF_ZONE_API_TOKEN: ${CLOUDFLARE_TOKEN}
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock:ro'
      - 'traefik_letsencrypt:/letsencrypt'
      - './traefik.yml:/etc/traefik/traefik.yml'
    ports:
      - '80:80'
      - '443:443'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    networks:
      - traefik

  portainer:
    image: portainer/portainer-ce:2.6.2-alpine
    container_name: portainer
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.portainer.rule=Host(`portainer.${DOMAIN:-test}`)'
      - 'traefik.http.routers.portainer.entrypoints=websecure'
      - 'traefik.http.routers.portainer.tls=true'
      - 'traefik.http.services.portainer.loadbalancer.server.port=9000'
    restart: always
    extra_hosts:
      host.docker.internal: host-gateway
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
      - 'portainer_data:/data'
    networks:
      - traefik

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.prometheus.rule=Host(`prometheus.${DOMAIN:-test}`)'
      - 'traefik.http.routers.prometheus.entrypoints=websecure'
      - 'traefik.http.routers.prometheus.tls=true'
      - 'traefik.http.services.prometheus.loadbalancer.server.port=9090'
    networks:
      - traefik

volumes:
  portainer_data:
  traefik_letsencrypt:

networks:
  traefik:
    external: true
